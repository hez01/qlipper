qlipper (1:6.1.0-6) unstable; urgency=medium

  * The history list is now a scroll area: scrolling moves the entries
    within a fixed viewport instead of repositioning the whole menu to the
    top of the screen. The menu also resizes to fit after filtering.

 -- Local Build <local@localhost>  Tue, 22 Sep 2026 17:23:01 +0000

qlipper (1:6.1.0-5) unstable; urgency=medium

  * "Items shown in menu" now controls how many entries are visible before
    the menu scrolls, rather than hiding the rest: all entries stay in the
    menu and the overflow is reachable by scrolling.

 -- Local Build <local@localhost>  Tue, 22 Sep 2026 17:08:58 +0000

qlipper (1:6.1.0-4) unstable; urgency=medium

  * The history menu now anchors its top-left corner at the mouse cursor
    (menu opens downwards/rightwards) instead of flipping upwards near the
    bottom of the screen. X11 only; on Wayland the compositor decides
    window placement.

 -- Local Build <local@localhost>  Tue, 22 Sep 2026 16:44:35 +0000

qlipper (1:6.1.0-3) unstable; urgency=medium

  * Search now matches the entire clipboard content of each entry, not
    just the truncated preview text shown in the menu.
  * The clipboard history menu can now scroll instead of overflowing off
    the screen, and image thumbnails / entry icons are shown larger.
  * New "Menu Appearance" preferences: item image size, item text size,
    and how many entries are shown in the menu at once (search still
    scans the whole history regardless of that cap).
  * The first entry in the menu is now auto-selected on open and while
    filtering, so typing a query and pressing Enter activates the top
    match. Enter always falls back to the first entry.
  * Builds on Ubuntu 24.04: KF6 KSystemClipboard is now optional and
    falls back to QClipboard when KF6 GuiAddons is not installed.

 -- Local Build <local@localhost>  Tue, 22 Sep 2026 16:35:49 +0000

qlipper (6.1.0-2) unstable; urgency=medium

  * Migrate dynamic-history storage from a QSettings array to SQLite
    (adds a Qt6 Sql build/runtime dependency). Every insert, move-to-top,
    trim, and per-entry delete is now a single targeted statement instead
    of rewriting the whole history to disk, which is what made an
    unlimited/large history impractical before.
  * History size limit can now be set to 0 (Unlimited) in preferences.
  * Delete key in the search menu removes the highlighted entry.
  * Existing QSettings-stored history is migrated into SQLite
    automatically on first run, then removed from the settings file.
  * Removed the now-meaningless "Synchronize history to storage
    instantly" preference: single-row SQLite writes make that
    distinction moot.

 -- Local Build <local@localhost>  Tue, 22 Sep 2026 06:09:37 +0000

qlipper (6.1.0-1) unstable; urgency=medium

  * Add a live search bar to the clipboard history tray menu, with
    keyboard navigation (Up/Down/Enter/Escape).
  * Show copied images in the history again via cached thumbnails.
    Full-resolution image bytes go to a content-addressed on-disk
    cache instead of being re-serialized into the settings file on
    every clipboard change (that repeated full rewrite was the cause
    of the old freeze with images in history).
  * Stop recording other, unhandled binary clipboard content.
  * History size limit can now be set to 0 (Unlimited) in preferences.
  * Delete key in the search menu removes the highlighted entry.

 -- Local Build <local@localhost>  Tue, 22 Sep 2026 05:59:51 +0000
